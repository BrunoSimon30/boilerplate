/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com"], // 👈 Allowed image domains
  },
  reactStrictMode: true,
};

export default nextConfig;
